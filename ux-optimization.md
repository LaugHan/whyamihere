# Why Am I Here? 使用体验优化提案

> 版本：2026-08-31 · 基于 commit `494ea0e`（测试基线 76/76 通过）
> 配套交互稿：[`mockups/overlay-redesign.html`](mockups/overlay-redesign.html)（浏览器直接打开即可体验「现行版 vs 改进版」对比）

---

## TL;DR

**产品的理念是 one sec，交互却做成了 Cold Turkey。**

README 说「它不是 blocker，而是 intention checkpoint」，但实际交互是：全屏遮罩 + 强制写满 50 字作文才能解锁，且没有任何出口。诚实的成本太高（写 50 字），而产品最有价值的时刻——**「我写不出来」**——没有任何 UI 支持。

五个竞品/研究给出的核心结论高度一致：

1. **暂停本身就是干预**。one sec 只要求呼吸一口气 + 一次确认，就让 App 打开率降了 57%（Max Planck / 海德堡大学，PNAS 2023）。不需要作文。
2. **摩擦应该放在「跳过」上，而不是「回答」上**。StayFocusd 的打字挑战、ScreenZen 的自定义词，都是让「绕过」变慢；回答则要尽量便宜。
3. **固定的干预会衰减**。HabitLab（Stanford）实测：静态干预随天数效果下降，轮换干预 + 讲解轮换机制能显著降低流失。
4. **每个选择要有真实出口**。Purpose Mode（CHI 2025）证明「给用户控制感」本身就降分心感（28% → 7%）。
5. **数据闭环是留存引擎**。ScreenZen 统计「暂停多少次让你改变了主意」；Forest 靠成就系统留住用户。

据此，最高优先级的改动只有一句话：

> **把「写 50 字作文才能走」改成「一秒钟做出诚实的三选一：有目的 / 没目的 / 正事中」。**

---

## 1. 竞品调研

### 1.1 一览表

| 产品 | 平台 | 机制 | 关键数据 / 结论 | 对我们的启示 |
|---|---|---|---|---|
| **one sec** | iOS / Android / 浏览器扩展 | 打开目标 App 前插入呼吸练习 + 意图确认 | 与 Max Planck、海德堡大学的同行评审研究：目标 App 打开率 **-57%**；Trier 大学 2022 研究：简单延迟即 -36% | 「暂停」本身就是干预，意图确认可以轻到一次点击 |
| **ScreenZen** | iOS / Android / Mac（免费） | 倒计时摩擦 + 可选「输入理由」+ **递增延迟**（当天开得越多等得越久）+ 统计 | 免费捐赠制，4.8 星 3 万+ 评价；多数用户稳定在约 -30% 用量；「仪式化」（习惯化）是主要失效原因 | ① 「多少次暂停改变了你的主意」是极好的正反馈指标；② 递进摩擦对付惯犯；③ 免费不收费可以攒口碑 |
| **HabitLab**（Stanford） | Chrome | 多样化干预轮换（去信息流、看前确认、评论隐藏…）+ 目标设定 | 干预频繁的周，目标站点时间浏览器端 -8%、移动端 -37.3%；**静态干预随时间衰减，轮换 + 每次介绍新干预并解释机制，显著降低流失** | 轮换问题文案 / 形式，防习惯化；「今天第 N 次」这类上下文有唤醒作用 |
| **News Feed Eradicator** | Chrome / Firefox | 直接移除信息流，替换为一句名言；带 snooze | 多年老牌扩展，口碑核心是「改变环境而不是每次靠意志力」 | 可作为未来的「模式 2」（环境模式）而非默认；snooze 是标配 |
| **StayFocusd** | Chrome | 每日时间预算 + Nuclear Option + **改设置需通过打字挑战** | 「把摩擦放在改设置上」的代表作；Nuclear Option 面向「知道自己会破戒」的用户 | 我们的对应物：长按跳过 / 连续跳过需确认，而**作答永远一键** |
| **Purpose Mode**（CHI 2025） | Chrome（研究原型） | 可开关的「注意力捕获模式」（关无限滚动、自动播放、推荐） | 29 人实地实验：分心感 28% → 7%，每天少刷 21 分钟 | 「控制感（agency）」本身有效；把「选择权」还给用户比强制更持久 |
| **stop-mess-around** | Chrome（国内开源） | 检测摸鱼站 → 显示激励语 → **直接关闭页面** | 纯强制路线，用户反馈集中在「误伤正事」 | 国内桌面端确实缺「轻量意图检查点」品类；强制关闭路线的坑不用再踩 |
| **BiliFocus** | Chrome / Edge（国内开源） | B 站 UI 净化（隐藏推荐、 Shorts 等） | 单站净化路线，维护成本高 | 同类需求的另一种解法，佐证需求真实存在 |

### 1.2 与本项目直接相关的三个市场判断

1. **「意图检查点」在浏览器端是空位。** one sec 的评测明确指出它在浏览器里什么都不做（只拦原生 App），而浏览器恰是桌面摸鱼的主战场。本项目的定位（intention checkpoint）在桌面 Chrome 生态没有正面对标物，这是机会。
2. **one sec 型产品的两大失效模式我们都会遇到**：① 「每次都弹」导致的烦躁与习惯化（多篇评测共同吐槽）；② 摩擦只挡「无意识打开」，挡不住「铁了心要刷」。对应的解法分别是：分级/轮换干预 + 递进摩擦，而不是一味加码封锁。
3. **免费 + 本地 + 无账号是口碑杠杆。** ScreenZen（全免费）3 万+ 评价 4.8 星；本项目「数据全部本地」是现成卖点，应该在权限和文案上兑现（见 P2-2）。

> 参考链接：[one sec 评测（含 57% 数据）](https://www.screenbuddyapp.com/blog/one-sec-app-review)、[one sec 机制分析（含 Trier -36%）](https://www.browwwser.com/resources/one-sec-app-review-2026/)、[one sec 官方背景](https://faith.tools/app/353-one-sec)、[ScreenZen 评测](https://unhookd.app/blog/screenzen-worth-it-review)、[ScreenZen 免费模式与习惯化讨论](https://habitdoom.com/blog/screenzen-alternative-iphone)、[「输入理由杀灭冲动」](https://pausegadget.com/best-app-blockers/)、[HabitLab 论文（干预频率实验）](https://hci.stanford.edu/publications/2019/conservation/conservation-chi2019.pdf)、[HabitLab 学位论文（轮换降流失）](https://stacks.stanford.edu/file/druid:qq438qv1791/Thesis-augmented.pdf)、[HabitLab 仓库](https://github.com/habitlab/habitlab)、[News Feed Eradicator](https://chrome-stats.com/d/news-feed-eradicator)、[StayFocusd](https://tooltivity.com/extensions/stayfocusd-website-blocker-and-focus-timer)、[StayFocusd 评测（Nuclear Option / 打字挑战）](https://www.pcmag.com/archive/stayfocusd-269215)、[Purpose Mode（CHI 2025）](https://dl.acm.org/doi/10.1145/3711841)、[stop-mess-around](https://blog.csdn.net/OBKoro1/article/details/118199312)、[BiliFocus](https://www.bilibili.com/video/BV12XKo6REFN/)

---

## 2. 现状走查：体验断点清单

按严重程度排序。行号基于当前 `why-am-i-here/` 代码。

### 🔴 A 级（伤害核心理念 / 直接驱动卸载）

| # | 断点 | 现状（代码依据） | 后果 |
|---|---|---|---|
| A1 | **全屏遮罩、零出口** | `content.js:33` `.overlay-backdrop` 铺满全屏（`rgba(0,0,0,.35)`，z-index 2147483646），页面不可交互；面板没有关闭/跳过按钮 | 一个「不是 blocker」的产品，实际交互比 blocker 更硬：blocker 至少让你走，这里必须交 50 字「作文税」才能走。误伤正事（看一个 2 分钟的知乎长回答、B 站查一个教程）时愤怒值最高，这是卸载/停用插件的第一动机 |
| A2 | **「写不出来」没有出口** | motivation 里写「如果写不出来，这个弹窗已经帮你意识到」——但 UI 里没有「我没目的」的任何按钮 | 产品最有价值的时刻（觉察到自己无目的）不被 UI 支持，用户只能在「编 50 字」和「手动关标签页」之间选。工具最终训练的是**编造理由**，与初衷相反 |
| A3 | **50 字门槛太重，且示例自相矛盾** | `content.js:6` `DEFAULT_MIN_CHARS = 50`；`content.js:227` placeholder 示例「我想查找关于机器学习的整理贴，收集资料...」去空格后只有约 18 字 | 用户照着官方示例写，写完发现提交不了——**第一个使用时刻就是挫败时刻**。50 字会催生凑字废话，历史记录变成噪声，「意图」失去信号价值 |
| A4 | **焦点劫持** | `content.js:350` `setTimeout(() => textarea.focus(), 400)` | 用户正在页面里打字（写评论、搜索框输入）时，弹窗 0.4 秒后抢走键盘，正在输入的字被打断并敲进 textarea。突兀且具有破坏性 |

### 🟠 B 级（明显摩擦 / 信任成本）

| # | 断点 | 现状 | 后果 |
|---|---|---|---|
| B1 | **无预警、无上下文** | 60 秒整点直接全屏跳变 | 「惊吓式」而非「预告式」。第 1 次和第 100 次的文案完全相同，没有任何「今天第 N 次」的觉察信息 → 快速习惯化（HabitLab 已证明静态干预衰减） |
| B2 | **免打扰只有按域名** | `background.js` snoozeMap 以 domain 为 key | 跨站查资料（知乎→B站→微博）会被逐站打断；没有「我在专注，全局别烦我」 |
| B3 | **权限面过大** | `manifest.json:6-7`：`tabs` + `host_permissions: <all_urls>`，content script 注入所有网站 | 安装弹窗会显示「读取和更改您在所有网站上的数据」——约 35% 的扩展请求了这一最宽权限，也是安全类文章列出的第一大红旗。对「隐私全本地」的产品是自我否定，直接影响商店转化 |
| B4 | **安装后无引导** | onInstalled 只写默认值，无欢迎页 | 用户装完不知道它在不在工作；演示模式（5 秒/10 字）藏在 popup 第二屏，黑客松演示完忘关，扩展永远处于 5 秒狂弹状态 |

### 🟡 C 级（打磨项 / 已知问题）

| # | 断点 |
|---|---|
| C1 | 中英文计数不公平：`content.js:271` 按去空格字符数计，英文 50 字符 ≈ 10 个单词，比中文难写得多 |
| C2 | 无键盘路径：Enter 不能提交、Esc 无行为 |
| C3 | 改设置不即时生效（README 已知）；`chrome.alarms` 在正式版有 30 秒下限，演示模式的 5 秒在生产环境实际约 30 秒才触发 |
| C4 | 计时只统计「本标签页连续前台时间」：切走再切回计时归零，Alt-Tab 无意识切换永远不被捕获；也没有「按天累计」模式 |
| C5 | 多窗口串扰：`background.js` 只维护单个 `activeTabId`，双窗口下计时归属会乱 |
| C6 | 历史页：500 条无分页无搜索、删除无确认无撤销；popup 移除域名同样无确认 |
| C7 | 反馈闭环缺失：没有任何统计（README 已列为 roadmap），用户感知不到工具的价值→留存差 |

---

## 3. 改进方案

### 3.0 五条设计原则（全部来自上面的调研）

1. **暂停即干预，作答要便宜**（one sec：-57% 靠的是一口气，不是作文）
2. **摩擦不对称：诚实一秒钟，跳过要刻意**（StayFocusd 打字挑战 / ScreenZen 自定义词）
3. **每个选择都要有真实出口**（Purpose Mode：控制感本身有效）
4. **形式要变化，防习惯化**（HabitLab：轮换 + 解释机制降低流失）
5. **让用户看见自己的变化**（ScreenZen「改变主意的次数」/ Forest 成就）

### 3.1 P0 —— 弹窗的「三岔路口」重设计（1~2 天，决定留存）

**目标：把单选题（写 50 字）变成三岔路口，每个路口都有真实出口。**

#### P0-1 三条路径（核心）

```
┌──────────────────────────────────────────────┐
│  为什么在这里？              [今天第 3 次]     │
│                                              │
│  ① 查资料  ② 看关注的更新  ③ 找具体问题       │ ← 点选即达标（也可手写≥10字）
│  ┌────────────────────────────────────────┐  │
│  │ 或写一句（至少 10 字）…                  │  │
│  └────────────────────────────────────────┘  │
│  之后 [15分][30分][1小时*][2小时][4小时]       │ ← 免打扰时长就地可选（默认记住上次）
│      内不再打扰                               │
│  [ 继续浏览 ]                                ← 主路径：一键完成
│                                              │
│  ───────── 或者 ─────────                    │
│  [ 我没目的，关掉它 ]   [ 限时放风 ▾ ]         ← 出口 1/2
│                          └ 3 / 5 / 10 分钟   │ ← 点时长即开始，到点自动再问
│                                              │
│  长按 2 秒跳过本次（不记录）                   ← 摩擦在跳过，不在回答
└──────────────────────────────────────────────┘
```

- **路径一（有目的）**：预设意图 chips 一键点选，或手写。门槛降为 **10 字默认**。免打扰时长作为一排小 chips 就地放在主按钮上方（15 分 / 30 分 / 1 小时 / 2 小时 / 4 小时，记住上次选择，默认 1 小时），主按钮文案跟随所选时长——既保留一键完成，又不再把「时长」做成一个必经的独立步骤。
- **路径二（没目的）**：主按钮「关掉它」直接 `chrome.tabs.remove()`（或回到上一个非监控标签页）；次按钮「限时放风 ▾」展开时长选择（3 / 5 / 10 分钟，点时长即开始）＝ 有界刷（bounded grazing，one sec 的 1 分钟模式），倒计时结束后自动再问一次。**这是产品的灵魂时刻，必须显式存在。**
- **路径三（长按跳过）**：长按 2 秒带进度条，本次放行、不写历史。跳过必须慢（摩擦不对称），但永远可行（保持「不是 blocker」的承诺）。
- 递进策略联动：同一域名当天第 3 次起，免打扰选项自动变短（最长 30 分钟、默认 15 分钟），放风最长 5 分钟，长按跳过从 2 秒升到 4~5 秒——摩擦随复发递增，回答永远一键。

历史记录随之增加一个字段 `outcome: 'purpose' | 'no_purpose_close' | 'graze' | 'skip'`——这是后统计计「救回次数」的基础。

#### P0-2 门槛与计数修正

- 默认 50 → **10 字**；placeholder 与门槛一致（示例必须能提交）。
- 计数规则改为：CJK 字符 1 字 / 连续英文单词 1 字 / 数字串 1 字，中英文公平。
- 保留「严格模式」选项（50 字）给自愿硬核的用户。

#### P0-3 非模态化 + 焦点礼仪

- 去掉全屏 backdrop（或仅保留极轻的右上暗角提示），页面保持可滚动。
- 仅当 `document.activeElement` 不是输入框时才自动聚焦 textarea；绝不抢正在打字的用户。
- 键盘：`Ctrl/Cmd+Enter` 提交；`Esc` 呼出次级选择（放风 / 关闭 / 长按跳过）。

#### P0-4 上下文徽标 + 文案轮换

- 面板顶部显示「今天第 N 次在这里 / 本周第 M 次」——觉察数据本身就是干预。
- 准备 4~5 套问题文案轮换：「你为什么打开这个网站？」「你现在最该做的一件事是什么？」「这是你今天第 3 次在这里」「这一次和上一次的理由一样吗？」（HabitLab：变化防衰减）。

### 3.2 P1 —— 让打断变聪明（约一周）

| # | 改动 | 说明 |
|---|---|---|
| P1-1 | **预警胶囊（分级干预）** | 达到延迟的 ~60% 时，右上角出现小胶囊：「约 24 秒后会问你为什么在这里 · [现在就答] [今天先不问]」。预告代替跳变，给正事用户一个便宜的提前出口；胶囊本身可忽略（不打断） |
| P1-2 | **意图 chips 自学习** | 从历史记录统计该域名高频意图，动态生成 chips；「随便刷刷」永远保留一项（诚实出口常驻） |
| P1-3 | **递进策略**（ScreenZen progressive） | 同一域名当天第 3 次起：免打扰预设整体变短（1h→15m）、问题文案升级为「今天第 3 次了，和上午的理由一样吗？」；长按跳过从 2 秒升到 5 秒。摩擦随复发递增，回答永远一键 |
| P1-4 | **全局专注免打扰** | 免打扰选项增加「全部网站」；popup 加一键「接下来 1 小时别烦我」 |
| P1-5 | **触发模式可选** | 三种：① 连续停留（现状）② 当日累计时长（StayFocusd 式预算，堵住 Alt-Tab 归零漏洞）③ 滚动深度（信息流感测，实验性）。同时把计时下沉到 content script（`setInterval` + `visibilitychange`），绕开 `chrome.alarms` 30 秒下限，并顺手砍掉 `tabs` 权限（见 P2-2） |
| P1-6 | **Onboarding + 演示模式自动还原** | 安装后打开欢迎页：30 秒讲理念 → 勾选域名 →「立即体验一次」（复用演示模式）→ 明确显示「演示模式将于 10 分钟后自动关闭」。演示模式的 5 秒/10 字不再永久驻留 |

### 3.3 P2 —— 留存与信任（两周内）

| # | 改动 | 说明 |
|---|---|---|
| P2-1 | **统计面板** | 历史页顶部：今日打断次数、无目的占比、**「救回次数」**（点了关掉它的次数）、放风使用次数、周趋势条形图。「救回 N 次」是本产品独有的正反馈指标（对标 ScreenZen 的「改变主意的次数」）。附带：历史搜索、按日分组、导出 JSON/CSV（roadmap 已有） |
| P2-2 | **权限收敛** | content_scripts 只匹配监控域名；用户自定义域名用 `optional_host_permissions` 按需申请；计时下沉 content script 后去掉 `tabs` 权限。安装提示从「读取所有网站数据」变成具体域名列表。同时在商店文案显著强调「无服务器、无账号、数据永不离开你的浏览器」——这是对标 one sec（订阅制）的差异化卖点 |
| P2-3 | **popup 今日摘要** | 打开即见「今天被打断 3 次 · 救回 1 次 · 距上次打扰 42 分钟」，把检查扩展从「设置面板」变成「成果面板」 |

### 3.4 明确不建议做的

- ❌ **账号 / 云同步**：隐私是核心卖点，也是相对 one sec 订阅制的差异化。
- ❌ **强制封锁模式**：「不是 blocker」是定位，别把自己拉进 Freedom/Cold Turkey 的红海比谁更狠。
- ❌ **TODO / 番茄钟 / 日程全家桶**：motivation.txt 说得很清楚——第一步没迈出去时，时间管理系统全都没用。克制是这个产品的美德。
- ❌ **羞辱式文案 / 音效**：与「它不骂你」的承诺冲突，且是这类产品差评的高发区。

---

## 4. 交互稿

打开 [`mockups/overlay-redesign.html`](mockups/overlay-redesign.html)（无需服务器，双击打开），在一个仿知乎信息流页面里：

1. 点 **「体验现行版」**：感受 50 字作文 + 全屏遮罩 + 无出口（placeholder 示例写进去也提交不了——A3 的实感）。
2. 点 **「体验改进版」**：完整走一遍新流程——预警胶囊倒计时 → 三岔路口面板 → 三条路径（含「限时放风」倒计时结束后自动再问、长按 2 秒跳过）→ 顶部统计条实时变化。
3. 注意对比：改进版不铺全屏遮罩、页面可滚动、文案带「今天第 N 次」上下文。

---

## 5. 数据与隐私：到底记录了什么

**原则：记录的目的是让用户看见自己的模式，而不是监督用户。所有数据只在本机，零网络请求**（已用代码检索确认：`fetch` / `XMLHttpRequest` / `WebSocket` / `sendBeacon` / `analytics` 在整个扩展中零匹配）。

| 你的动作 | 现版扩展 | 改进方案（P0 后） | 存在哪 |
|---|---|---|---|
| 写目的并提交 | ✅ 记录：域名、URL、全文、免打扰时长、时间戳 | ✅ 同左（chips 点选=目的本身，同样记录） | `chrome.storage.local`（仅本机），上限 500 条，历史页可单条删除 |
| 「我没目的，关掉它」 | ——（该路径不存在） | ⚠️ 记录**事件**：域名 + 时间 + outcome，**无任何文字**（用于「救回次数」统计） | 同上 |
| 「限时放风」 | —— | ⚠️ 记录事件：域名 + 时长 + 时间 | 同上 |
| 长按跳过 | —— | ❌ **不写入历史**（保持低心理负担）；只递增一个本地计数器供「跳过占比」指标使用，用户可见、可清零 | 本地计数器 |
| 没写完就关页面 / 切走 | 不记录 | 不记录 | —— |
| 设置（域名列表、延迟、字数） | `chrome.storage.sync` | 建议改为可选 local：sync 会经 Google 账号跨设备同步（不是开发者服务器，但严格说「不出本机」不成立） | sync 或 local |
| 免打扰状态 | 本机 | 本机 | `chrome.storage.local` |

配套改动建议：

1. **历史页加「清空全部」（带确认）**；现版只能一条条删。
2. **安装引导里用一句话讲清楚**：「记录只存在你的浏览器里，永不上传，可随时删除」——这既是事实也是卖点。
3. **「救回 / 放风 / 跳过」计数器单独可清零**，和文字历史分离：文字历史是给用户回顾的，计数器只是统计，敏感度不同。

---

## 6. 怎么知道改对了（度量）

| 指标 | 目标 | 含义 |
|---|---|---|
| 作答时长中位数 | < 10 秒 | 暂停是否足够便宜（one sec 标准） |
| 「无目的 → 关掉它」比率 | 10%~25% | 产品核心价值发生率；过高=误伤正事（该调延迟），过低=门槛太松 |
| 长按跳过占比 | < 30% | 摩擦不对称是否成立；持续走高 = 习惯化预警 |
| 第 7 天弹窗完成率 | > 60% | 习惯化程度（HabitLab 衰减曲线的本地版） |
| 历史记录里「理由」的平均长度 | 自然分布而非扎堆 50 | 作文税是否真的取消了 |

埋点全部来自本地已有的 history 字段（加上 `outcome`），不需要任何网络上报。

---

## 7. 附：实施顺序建议

| 阶段 | 内容 | 预估工作量 |
|---|---|---|
| 第一周 | P0-1 ~ P0-4（弹窗重设计 + 非模态 + 计数）+ C1/C2 | 2~3 天 |
| 第二周 | P1-1 / P1-2 / P1-4 / P1-6 | 3~4 天 |
| 第三周 | P1-3 / P1-5 / P2-1 / P2-3 | 4~5 天 |
| 发布前 | P2-2 权限收敛 + 商店文案 | 2 天 |

P0 全部可以只动 `content.js` + `popup.js`（新增 outcome 字段动 `background.js` 的 SAVE_ENTRY），不破坏现有 76 个测试的主体逻辑；`run-tests.js` 中 minChars 相关断言需同步更新默认值 50 → 10。
